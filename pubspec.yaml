name: offline_videos_poc
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  background_downloader: ^7.4.0
  video_player: ^2.6.1
  hive_flutter: ^1.1.0
  flutter:
    sdk: flutter
  path_provider: ^2.0.15
  m3u:
    git:
      url: https://github.com/bmoliveira/m3u-parse-utils
  flutter_tencentplayer: ^2.0.1
  flutter_hls_parser: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
